/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.istockphoto.com', 't3.ftcdn.net', 'necessary-ladybug-115.convex.cloud', 'upload.wikimedia.org', 'images.unsplash.com', 'charming-clownfish-726.convex.cloud'],
    },
};

export default nextConfig;
